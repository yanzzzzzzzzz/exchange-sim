spring:
  datasource:
    url: jdbc:h2:mem:testwalletdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

logging:
  level:
    wallet: DEBUG